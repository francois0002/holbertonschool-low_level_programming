#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 * Description: afficher alphabet a l'envers
 * Return:  0
 */
	 int main(void)
{
int alpha;

for (alpha = '0' ; alpha = 8 ; alpha++)
{
putchar (alpha);
}
putchar ('\n');
return (0);
}
