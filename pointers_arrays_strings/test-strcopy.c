#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
 

 
int main(void)
{
    char *ptr;

    ptr = ("First, solve the problem. Then, write the code\n");
    printf("%s", ptr);
    return (0);
}
