#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
 
 int recursion_fibonnaci(int x)
 {
	 int sum = 0;
	 
	 if ()
	 {
		sum = sum + x;
		sum = recursion_fibonnaci(x+sum);
	 }
 	return(sum);
 }
 
 
 
int main(void)
{
recursion_fibonnaci(5);

    return (0);
}
