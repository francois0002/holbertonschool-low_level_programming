#include <stdio.h>
#include <math.h>
/**
 * is_prime_number- Entry point
 * Description: racine carré
 * @n : nombre à choisir
 * Return: resultat
 */


int is_prime_number(int n)
{

	if (n == 0)
	{
		return (0);
	}
	if (n < 0)
	{
		return (-1);
	}
		if (n < 0)
	{
		return (-1);
	}
	else
	{
	return (0);
	}
}
