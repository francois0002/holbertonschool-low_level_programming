#include <stdio.h>
/**
 * _sqrt_recursion- Entry point
 * Description: racine carré
 * @n : nombre à choisir
 * Return: resultat
 */


int _sqrt_recursion(int n)
{

	if (n == 0)
	{
		return (0);
	}
	if (n < 0)
	{
		return (-1);
	}
	else
	{
	return (-1);
	}
}
