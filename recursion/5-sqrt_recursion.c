#include <stdio.h>
/**
 * _sqrt_recursion- Entry point
 * Description: racine carré
 * @n : nombre à choisir
 * Return: resultat
 */


int _sqrt_recursion(int n)
{

	if (n == 0)
	{
		return (0);
	}
	if (n < 0)
	{
		return (-1);
	}
		if (n < 0)
	{
		return (-1);
	}
	
	
	
	if (y * y = n)
	{
	return(_sqrt_recursion(n / fonction_multiplication(y);
	}
}

int fonction_multiplication(int y)
{
y = 1;
int resultat;
resultat = y * y;
y++;
return (resultat);
}
